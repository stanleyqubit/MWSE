return {
	type = "value",
	description = [[Selects the + or - axis direction of the object that points along the path. Combines with `followAxis` to fully specify the axis.]],
	valuetype = "boolean",
}
