return {
	type = "function",
	description = [[Returns the command line array for the currently execution. This almost always has one value, which is the (potentially relative) path to Morrowind.exe]],
	returns = {
		{ name = "commandLine", type = "string[]", description = "An array of strings that reprents the command line." },
	},
}
