return {
	type = "value",
	description = [[True if the file is flagged to load. Flagging a file to load will overwrite the user's load options, and should not be done lightly.]],
	valuetype = "boolean",
}
