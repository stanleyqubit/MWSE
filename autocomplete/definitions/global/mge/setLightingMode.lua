return {
	type = "function",
	description = [[Sets the lighting mode used by MGE XE. The values passed can be used from the [`mge.lightingMode`](https://mwse.github.io/MWSE/references/mge/lighting-modes/) constants.]],
	arguments = {
		{ name = "mode", type = "integer", description = "Maps to the `mge.lightingMode` constants." },
	},
}
