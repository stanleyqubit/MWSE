return {
	type = "function",
	description = [[Gets the MGE version. Returns a table with keys `major`, `minor`, and `patch`. In some older MGE XE versions, the versions are concatenated to a string.]],
	returns = "mgeVersionTable|string",
}